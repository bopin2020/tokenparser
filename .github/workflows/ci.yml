name: Scheduled Job

on:
  schedule:
    - cron: '*/5 * * * *'  # 每 5 分钟运行一次

jobs:
  run-every-5-minutes:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Run a script
        run: echo "This job runs every 5 minutes!"